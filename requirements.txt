# LangGraph + LangChain
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.3.0

# LangGraph Checkpointer (PostgreSQL)
langgraph-checkpoint-postgres>=2.0.0
psycopg[binary]>=3.0.0

# Web UI (FastAPI + HTMX)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
jinja2>=3.1.0
python-multipart>=0.0.9
sse-starlette>=2.0.0
markdown>=3.5.0

# Web UI (Streamlit — legacy, optional)
# streamlit>=1.30.0

# Observability
langfuse>=2.0.0

# Dev Pipeline (file watcher)
watchdog>=4.0.0

# Config / Util
pyyaml>=6.0
python-dotenv>=1.0.0
